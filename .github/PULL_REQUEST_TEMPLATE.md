## What changed

## Why

## How tested

## Deliverable / task
<!-- Increment (I0–I5) and T-NNN. One PR per deliverable slice. -->

---
- [ ] Commits signed off (`git commit -s`) — DCO
- [ ] `bin/validate-archdoc` passes
- [ ] `git submodule status` — library pin intentional (if the submodule is wired)
- [ ] If this touches `spec/` or `project/`: `version` **and** `updated` both bumped, changelog appended
- [ ] **No `DEC-*` marked accepted here.** Only an `ADR-NNNN` accepts a decision.
- [ ] `design-log/` entry written in this change (if it produced spec/schema/mappings/vectors)
